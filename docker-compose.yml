services:
  heizung:
    build: .
    container_name: heizung
    restart: unless-stopped
    ports:
      - "5005:5005"
    volumes:
      - heizung_data:/data
    env_file:
      - .env

volumes:
  heizung_data:
